{
  "name": "seyir-movie-finder",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": { "dev": "node scripts/build-catalog.mjs && vite --host 0.0.0.0", "build": "node scripts/build-catalog.mjs && vite build", "preview": "vite preview --host 0.0.0.0" },
  "devDependencies": { "vite": "8.2.2" }
}
